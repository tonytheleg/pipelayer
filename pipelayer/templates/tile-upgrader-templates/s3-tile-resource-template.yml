- name: TILE_NAME-tile
  type: s3
  source:
    bucket: pcf-automation
    access_key_id: ((ecs_user.username))
    secret_access_key: ((ecs_user.password))
    endpoint: ((ecs_object_endpoint))
    regexp: tiles/INSERT_PRODUCT_SLUG_HERE-(.*).pivotal

